Testing:
-) testcases for the not covered functionality
-) testcase for CA cert rollover (conflictingTrustAnchors error)
-) implement NIST testsuite
-) FIX ...ns.OpensslDirTest - timeouts/bugs/or bugs in code??

Implementation:
-) OCSP support
-) noBasicConstraints - do we support v1 certs?
-) error codes: more full messages

Other:
-) review Grid Certificate Profile
-) Submit bug to BC: 
 In class org/bouncycastle/asn1/x509/GeneralName.java
 method:
    private void parseIPv4Mask(String mask, byte[] addr, int offset)
    {
        int   maskVal = Integer.parseInt(mask);

        for (int i = 0; i != maskVal; i++)
        {
            addr[(i / 8) + offset] |= 1 << (i % 8);
        }
    }
 is buggy. E.g. for mask=17 the result will be 255, 255, 1, 0 
 instead of 255,255,128,0
 Add it to known issues.